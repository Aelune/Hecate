
#  _   _ _____ ____    _  _____ _____
# | | | | ____/ ___|  / \|_   _| ____|     /\_/\
# | |_| |  _|| |     / _ \ | | |  _|      ( o.o )
# |  _  | |__| |___ / ___ \| | | |___      > ^ <
# |_| |_|_____\____/_/   \_\_| |_____|
#
# 🚀 KEYBINDS CONFIGURATION
# -----------------------------------------
# 📚 Wiki: https://wiki.hypr.land/Configuring/Binds/
#
# Syntax bind = MODS, key, dispatcher, params
# 💡 Quick Tips:
#    - Define shortcuts using `bind = SUPER,KEY,action`.
#    - Combine with `exec` for custom scripts.
#    - Organize by category (Window, System, Media...).
# -----------------------------------------

# Global variable
source = $HOME/.config/hypr/configs/UserConfigs/app-names.conf
$scriptsDir = $HOME/.config/hypr/scripts
$mainMod = SUPER
# ────────────────────────────────────────────────
#    SYSTEM / SESSION CONTROL
# ────────────────────────────────────────────────

bind = CTRL ALT, Delete, exec, hyprctl dispatch exit 0                     # Exit Hyprland
bind = CTRL ALT, L, exec, $scriptsDir/LockScreen.sh                        # Lock Screen
bind = $mainMod SHIFT, Q, exec, wlogout                                        # Logout Menu
bind = $mainMod ALT, R, exec, $scriptsDir/Refresh.sh                          # Refresh apps (waybar, swaync, etc.)
bind = $mainMod ALT, L, exec, $scriptsDir/ChangeLayout.sh                     # Toggle Layout (Master/Dwindle)
bind = $mainMod CTRL, W, exec, waypaper                                       # Wallpaper Selector
bind = $mainMod ALT, B, exec, pkill -SIGUSR1 waybar                      # Toggle Waybar Visibility
bind = $mainMod CTRL SHIFT, R, exec, pkill rofi || true && $scriptsDir/RofiThemeSelector-modified.sh

# ────────────────────────────────────────────────
#    APPLICATION LAUNCHERS
# ────────────────────────────────────────────────

bind = $mainMod, D, exec, pkill rofi || true && rofi -show drun -modi drun # App Launcher
bind = $mainMod, B, exec, $browser                                  # Launch Firefox
bind = $mainMod, C, exec, code                                      # Launch VSCode
bind = $mainMod, E, exec, thunar
bind = $mainMod, Return, exec, $term                                        # Launch Terminal
bind = $mainMod SHIFT, Return, exec, $scriptsDir/Dropterminal.sh kitty -e zsh # Dropdown Terminal

# bind = $mainMod, A, global, quickshell:overviewToggle
# bind = $mainMod, O, overview:toggle
bind = SUPER, o, hyprexpo:expo, toggle
bind = $mainMod, U, exec, kitty --title update-window --hold -e bash -c "$scriptsDir/install-updates.sh; exit"

# ────────────────────────────────────────────────
#    SCRIPTED FEATURES / USER UTILITIES
# ────────────────────────────────────────────────

bind = $mainMod ALT, E, exec, rofi -modi emoji -show emoji                      # Emoji Picker
bind = $mainMod, V, exec, $scriptsDir/ClipManager.sh                         # Clipboard Manager
bind = $mainMod SHIFT, N, exec, swaync-client -t -sw                         # Notifications Panel

# ────────────────────────────────────────────────
#    WORKSPACE MANAGEMENT
# ────────────────────────────────────────────────

# Switch workspaces
bind = $mainMod, tab, workspace, m+1
bind = $mainMod SHIFT, tab, workspace, m-1
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Standard workspace keys [1-10]
bind = $mainMod, code:10, workspace, 1
bind = $mainMod, code:11, workspace, 2
bind = $mainMod, code:12, workspace, 3
bind = $mainMod, code:13, workspace, 4
bind = $mainMod, code:14, workspace, 5
bind = $mainMod, code:15, workspace, 6
bind = $mainMod, code:16, workspace, 7
bind = $mainMod, code:17, workspace, 8
bind = $mainMod, code:18, workspace, 9
bind = $mainMod, code:19, workspace, 10

# Move window to workspace
bind = $mainMod, code:10, movetoworkspace, 1
bind = $mainMod, code:11, movetoworkspace, 2
bind = $mainMod, code:12, movetoworkspace, 3
bind = $mainMod, code:13, movetoworkspace, 4
bind = $mainMod, code:14, movetoworkspace, 5
bind = $mainMod, code:15, movetoworkspace, 6
bind = $mainMod, code:16, movetoworkspace, 7
bind = $mainMod, code:17, movetoworkspace, 8
bind = $mainMod, code:18, movetoworkspace, 9
bind = $mainMod, code:19, movetoworkspace, 10

bind = $mainMod SHIFT, bracketleft, movetoworkspace, -1
bind = $mainMod SHIFT, bracketright, movetoworkspace, +1

# Move window to workspace silently
bind = $mainMod, code:10, movetoworkspacesilent, 1
bind = $mainMod, code:11, movetoworkspacesilent, 2
bind = $mainMod, code:12, movetoworkspacesilent, 3
bind = $mainMod, code:13, movetoworkspacesilent, 4
bind = $mainMod, code:14, movetoworkspacesilent, 5
bind = $mainMod, code:15, movetoworkspacesilent, 6
bind = $mainMod, code:16, movetoworkspacesilent, 7
bind = $mainMod, code:17, movetoworkspacesilent, 8
bind = $mainMod, code:18, movetoworkspacesilent, 9
bind = $mainMod, code:19, movetoworkspacesilent, 10

bind = $mainMod CTRL, bracketleft, movetoworkspacesilent, -1
bind = $mainMod CTRL, bracketright, movetoworkspacesilent, +1

# Special workspace
# bind = $mainMod SHIFT, U, movetoworkspace, special
# bind = $mainMod, U, togglespecialworkspace

# ────────────────────────────────────────────────
#    WINDOW MANAGEMENT / LAYOUT
# ────────────────────────────────────────────────

# Focus movement
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Swap windows
bind = $mainMod ALT, left, swapwindow, l
bind = $mainMod ALT, right, swapwindow, r
bind = $mainMod ALT, up, swapwindow, u
bind = $mainMod ALT, down, swapwindow, d

# Move window (floating)
bind = $mainMod CTRL, left, movewindow, l
bind = $mainMod CTRL, right, movewindow, r
bind = $mainMod CTRL, up, movewindow, u
bind = $mainMod CTRL, down, movewindow, d

# Resize window
binde = $mainMod SHIFT, left, resizeactive,-50 0
binde = $mainMod SHIFT, right, resizeactive,50 0
binde = $mainMod SHIFT, up, resizeactive,0 -50
binde = $mainMod SHIFT, down, resizeactive,0 50

# Toggle floating
bind = $mainMod, SPACE, togglefloating
bind = $mainMod ALT, SPACE, exec, hyprctl dispatch workspaceopt allfloat

# Fullscreen
bind = $mainMod, F, fullscreen
bind = $mainMod CTRL, F, fullscreen, 1

# Groups
bind = $mainMod, G, togglegroup

# Master layout
bind = $mainMod CTRL, D, layoutmsg, removemaster
bind = $mainMod, I, layoutmsg, addmaster
bind = $mainMod, J, layoutmsg, cyclenext
bind = $mainMod, K, layoutmsg, cycleprev
bind = $mainMod CTRL, Return, layoutmsg, swapwithmaster

# Dwindle layggout
bind = $mainMod SHIFT, I, togglesplit
bind = $mainMod, P, pseudo
bind = $mainMod, M, exec, hyprctl dispatch splitratio 0.3
bind = $mainMod, N, exec, hyprctl dispatch splitratio -0.3

# ────────────────────────────────────────────────
#    MOUSE BINDINGS
# ────────────────────────────────────────────────

bindm = $mainMod, mouse:272, movewindow        # LMB drag = move
bindm = $mainMod, mouse:273, resizewindow      # RMB drag = resize

# Desktop zooming
bind = $mainMod ALT, mouse_down, exec, hyprctl keyword cursor:zoom_factor "$(hyprctl getoption cursor:zoom_factor | awk 'NR==1 {factor = $2; if (factor < 1) {factor = 1}; print factor * 2.0}')"
bind = $mainMod ALT, mouse_up, exec, hyprctl keyword cursor:zoom_factor "$(hyprctl getoption cursor:zoom_factor | awk 'NR==1 {factor = $2; if (factor < 1) {factor = 1}; print factor / 2.0}')"

# ────────────────────────────────────────────────
#    SCREENSHOTS
# ────────────────────────────────────────────────

# Keybinds for Screenshot Script

# Fullscreen Screenshot
bind = $mainMod, Print, exec, $scriptsDir/ScreenShot.sh full
# Active Window Screenshot
bind = $mainMod, SHIFT, Print, exec, $scriptsDir/ScreenShot.sh window
# Select Area Screenshot
bind = $mainMod CTRL, Print, exec, $scriptsDir/ScreenShot.sh area

# Extra: keybinding for keyboards with no print screen button
bind = $mainMod SHIFT, S, exec, $scriptsDir/ScreenShot.sh area


# ────────────────────────────────────────────────
#    MEDIA / VOLUME CONTROLS
# ────────────────────────────────────────────────

bindel = , xf86audioraisevolume, exec, $scriptsDir/Volume.sh --inc
bindel = , xf86audiolowervolume, exec, $scriptsDir/Volume.sh --dec
bindl = , xf86AudioMicMute, exec, $scriptsDir/Volume.sh --toggle-mic
bindl = , xf86audiomute, exec, $scriptsDir/Volume.sh --toggle

# Media playback
bindl = , xf86AudioPlayPause, exec, $scriptsDir/MediaCtrl.sh --pause
bindl = , xf86AudioPause, exec, $scriptsDir/MediaCtrl.sh --pause
bindl = , xf86AudioPlay, exec, $scriptsDir/MediaCtrl.sh --pause
bindl = , xf86AudioNext, exec, $scriptsDir/MediaCtrl.sh --nxt
bindl = , xf86AudioPrev, exec, $scriptsDir/MediaCtrl.sh --prv
bindl = , xf86audiostop, exec, $scriptsDir/MediaCtrl.sh --stop
