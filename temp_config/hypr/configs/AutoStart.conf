#  _   _ _____ ____    _  _____ _____
# | | | | ____/ ___|  / \|_   _| ____|     /\_/\
# | |_| |  _|| |     / _ \ | | |  _|      ( o.o )
# |  _  | |__| |___ / ___ \| | | |___      > ^ <
# |_| |_|_____\____/_/   \_\_| |_____|
#
# 🚀 AUTO-START CONFIGURATION
# -----------------------------------------
# 📚 Official Wiki: https://wiki.hypr.land/Configuring/Startup/
#
# 💡 Quick Tips:
#    - `exec-once` runs commands only at startup.
#    - `$variables` make paths easier to manage.
#    - Keep this minimal for faster login times.
#
# -----------------------------------------

# ────────────────────────────────────────────────
# DIRECTORIES & VARIABLES
# ────────────────────────────────────────────────
$scriptsDir     = $HOME/.config/hypr/scripts
$wallDIR        = $HOME/Pictures/wallpapers
$lock           = $scriptsDir/LockScreen.sh
$livewallpaper  = ""  # Optional: set live wallpaper command here

# ────────────────────────────────────────────────
# WALLPAPER DAEMON
# ────────────────────────────────────────────────
exec-once = swww-daemon --format xrgb

# ────────────────────────────────────────────────
# ENVIRONMENT SYNC (WAYLAND VARS)
# ────────────────────────────────────────────────
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

# ────────────────────────────────────────────────
# STARTUP APPLICATIONS
# ────────────────────────────────────────────────
exec-once = nm-applet --indicator         # Network Manager
exec-once = swaync                        # Notification daemon
exec-once = blueman-applet                # Bluetooth manager
exec-once = rog-control-center            # ASUS ROG Control Center
exec-once = waybar                        # Status bar
exec-once = qs                            # QuickShell (or AGS alternative)
# exec-once = ags                          # Uncomment if using AGS

# ────────────────────────────────────────────────
# CLIPBOARD MANAGER (CLIPHIST)
# ────────────────────────────────────────────────
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

# ────────────────────────────────────────────────
# LOCK & IDLE MANAGEMENT
# ────────────────────────────────────────────────
exec-once = hypridle                      # Idle detection with Hyprlock

# ────────────────────────────────────────────────
# PLUGIN LOADER (HYPRPM)
# ────────────────────────────────────────────────
exec-once = hyprpm reload -n              # Reload Hyprland plugins
